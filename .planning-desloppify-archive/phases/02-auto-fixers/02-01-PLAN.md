# Plan 02-01: Run All Auto-Fixers (Dry-Run, Review, Apply, Verify)

## Situation

Phase 1 resolved the single security finding. Now we apply desloppify's auto-fixers to clear as many findings as possible before manual cleanup phases.

### Current Scan Results (Post Phase 1)

- **unused** (imports/vars/params): 0 findings — already clean
- **exports**: 75 dead exports across 36 files (10 shown, 65 hidden by noise budget)
- **logs**: 1 tagged debug log in `src/gsd/phase-transition-cli.ts`
- **smells**: 24 findings (10 shown, 14 hidden) — but the auto-fixable subset (dead-useeffect, empty-if-chain) shows 0 findings

### Fixer Availability

| Fixer | Findings | Status |
|-------|----------|--------|
| `unused-imports` | 0 | Nothing to fix |
| `unused-vars` | 0 | Nothing to fix |
| `unused-params` | 0 | Nothing to fix |
| `dead-exports` | 75 | **Ready** — dry-run shows 75 de-exports across 36 files |
| `debug-logs` | 1 | **Broken** — `ImportError: cannot import name 'fix_debug_logs'` (desloppify bug) |
| `dead-useeffect` | 0 | Nothing to fix |
| `empty-if-chain` | 0 | Nothing to fix |

**Only `dead-exports` has work to do.** The `debug-logs` fixer crashes due to a missing import in desloppify itself — the 1 log finding must be fixed manually.

## Steps

### WP-01: Apply dead-exports fixer

The dry-run shows 75 exports to de-export across 36 files. The fixer removes the `export` keyword from symbols that have no external consumers. This is a safe, behavior-preserving change (internal references via same-module access still work).

1. **Run `desloppify fix dead-exports`** (apply mode, not dry-run)
2. **Verify build**: `npx tsc --noEmit` — type errors would indicate a consumer was missed
3. **Verify tests**: `npx vitest run` — behavioral regressions
4. **Verify bundle**: `node build.ts` — build still produces valid output

If tsc or tests fail, some exports DO have consumers that desloppify missed. In that case:
- `git diff` to identify which de-exports broke
- Re-add `export` to those specific symbols
- Report as false positives via `desloppify resolve <id> --status false_positive`

### WP-02: Manually fix the 1 debug-log finding

The `debug-logs` fixer is broken (desloppify bug). Fix the finding by hand.

**Finding**: `logs::src/gsd/phase-transition-cli.ts::phase-transition` — 1 tagged log at line 235.

1. **Read** `src/gsd/phase-transition-cli.ts` around line 235
2. **Identify** the tagged debug log (likely a `console.log` with `[phase-transition]` tag)
3. **Remove or convert** the log:
   - If it's debug-only: remove it
   - If it provides operational value: convert from `console.log` to the project's `logToFile()` utility
4. **Verify**: tsc + tests + build

### WP-03: Rescan and verify

1. **Run `desloppify scan`** to get updated scores
2. **Verify**:
   - unused findings: still 0
   - export findings: reduced (ideally 0 after dead-exports fixer)
   - log findings: 0
   - No new findings introduced
3. **Report** updated strict score vs baseline 69.6

## Scope

- **~36 files modified** by dead-exports fixer (removing `export` keyword)
- **1 file modified** manually (`src/gsd/phase-transition-cli.ts`) for debug log
- **0 files created/deleted**
- **No behavioral changes** — only visibility changes (export → local) and log removal

## Requirements Coverage

| Requirement | Disposition |
|-------------|-------------|
| AUTO-01 (unused-imports) | Already clean — 0 findings |
| AUTO-02 (unused-vars) | Already clean — 0 findings |
| AUTO-03 (dead-exports) | WP-01 — apply fixer |
| AUTO-04 (debug-logs) | WP-02 — manual fix (fixer broken) |
| AUTO-05 (rescan) | WP-03 — verify all resolved |

## Verification

- [ ] `desloppify fix dead-exports` applied successfully
- [ ] Debug log finding manually resolved
- [ ] Tests pass (`npx vitest run`)
- [ ] Types clean (`npx tsc --noEmit`)
- [ ] Build clean (`node build.ts`)
- [ ] `desloppify scan` shows reduced findings, no regressions
